# Features

This directory is intended for domain-specific feature modules like "Calendar", "Habits", "Analytics", etc.

Each feature folder should contain its own components, hooks, and services related to that specific domain.
